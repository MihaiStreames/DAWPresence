# AGENTS.md

## Agent skills

### Issue tracker

GitHub Issues at `MihaiStreames/DAWPresence`. Use `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical labels (defaults): `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo: `CONTEXT.md` + `docs/adr/` at repo root (created lazily). See `docs/agents/domain.md`.
